# CubeCubeBall 1.1.2 快速教程

1. 将 `cubeball-1.1.2.jar` 放入 `plugins`，服务器使用 Java 25 和 Luminol/Folia/Paper 26.2；本版本实际验证核心为 Luminol 26.2 build 726。
2. 启动服务器，管理员执行 `/ccb help` 确认权限，执行 `/ccb` 打开管理中心。
3. 点击“创建比赛”，在聊天框输入场地名。客户端聊天关闭时使用 `/ccb input <内容>`，使用 `/ccb input T` 可以取消输入。
4. 在场地配置页分别设置红队、蓝队出生点，使用石铲选择两个球门角点，设置足球出生点。
5. 可选设置原版足球方块、CraftEngine 外观、比赛时间、目标分数和冲刺冷却。
6. 点击“扫描玩家”让出生点附近玩家加入队伍，配置完成后点击“开始比赛”。
7. 玩家执行 `/ccb join 场地名` 进入等待大厅，右键背包中的红队、蓝队或观众物品选择身份。
8. 管理员修改 `config.yml` 后执行 `/ccb reload`。该命令异步加载通用配置、语言和视觉效果，不会替换进行中的比赛数据。

离线服保持 `player-identity.mode: name` 即可；正版服建议改为 `uuid`。管理员也可以在管理中心右下角点击名牌切换玩家识别方式。

视觉效果可在 `config.yml` 的 `visuals` 节点分别开关。高人数服务器建议增大 `ball-trail.interval-ticks` 或关闭足球尾迹。
